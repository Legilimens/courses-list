import cx from './index.module.css';

const Layout = () => {
  return (
    <main className={cx.flex}>
      <div>
        <h1 className={cx.h1}>Админка</h1>
      </div>
    </main>
  );
};

export default Layout;
